const Settings = () => {
  return (
    <div className="settings-page">
      <h1>Settings</h1>
      <p>Dashboard configuration and preferences will be displayed here.</p>
    </div>
  );
};

export default Settings;
